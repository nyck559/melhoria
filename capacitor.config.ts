import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.sololeveling.lifesystem',
  appName: 'SISTEMA',
  webDir: 'dist',
  backgroundColor: '#050308',
}

export default config
